{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "devCommand": "vite --port 3000",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}